devtools::install_github("MikeJSeo/bnma")
library(bnma)
network <- with(smoking, network.data(Outcomes = Outcomes, Study = Study, Treat = Treat, N = N, response = "binomial", mean.d = 0.1, hy.prior = list("dhnorm", 0, 5)))
str(network)
devtools::install_github("MikeJSeo/bnma")
devtools::install_github("MikeJSeo/bnma")
devtools::install_github("MikeJSeo/bnma")
install.packages("tibble")
install.packages("tibble")
devtools::install_github("MikeJSeo/bnma")
library(bnma)
network <- with(fake_data, network.data(Outcomes = Outcomes, Study = Study, Treat = Treat, N = N, response = "binomial", Treat.order = c("Placebo", "Treatment")))
network$Treat.order
network$Study.order
network <- with(smoking, network.data(Outcomes = Outcomes, Study = Study, Treat = Treat, N = N, response = "binomial", mean.d = 0.1, hy.prior = list("dhnorm", 0, 5)))
result <- network.run(network, n.run = 30000)
network <- with(cardiovascular, network.data(Outcomes, Study, Treat, N, response = "multinomial"))
result <- network.run(network)
summary(result)
str(network)
network <- with(certolizumab, network.data(Outcomes = Outcomes, Treat = Treat, Study = Study, N = N, response = "binomial", Treat.order = Treat.order, baseline = "common", baseline.risk = "independent"))
result <- network.run(network)
summary(result)
network$prior.data
